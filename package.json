{
  "name": "hazard-mons",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "snapshot:phase1": "npm run build && rm -rf docs/phase1 && cp -r dist docs/phase1",
    "snapshot:phase2": "npm run build && rm -rf docs/phase2 && cp -r dist docs/phase2",
    "snapshot:phase3": "npm run build && rm -rf docs/phase3 && cp -r dist docs/phase3",
    "sprites": "node scripts/generate-sprites.mjs"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}
